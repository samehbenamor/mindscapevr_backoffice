import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#b19cd9", // Snackbar background color
    },
    text: {
      primary: "#ffd3b5", // Text color
    },
  },
});

export default theme;